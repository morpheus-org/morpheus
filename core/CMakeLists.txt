MORPHEUS_SUBPACKAGE(Core)

IF (NOT Morpheus_INSTALL_TESTING)
  ADD_SUBDIRECTORY(src)
ENDIF()

MORPHEUS_ADD_TEST_DIRECTORIES(unit_test)
MORPHEUS_ADD_TEST_DIRECTORIES(perf_test)
MORPHEUS_SUBPACKAGE_POSTPROCESS()